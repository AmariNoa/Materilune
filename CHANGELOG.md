# Changelog

すべての注目すべき変更をこのファイルに記録します。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に基づいており、
このプロジェクトは [Semantic Versioning](https://semver.org/spec/v2.0.0.html) に準拠しています。

## [Unreleased]

### Added

### Changed

### Deprecated

### Removed

### Fixed

### Security

## [0.2.3] - 2026-08-18

### Fixed

- マテリアルプレビューのサムネイルが表示された後も、バックグラウンドの問い合わせが 100ms 間隔で続いていたのを止めました（候補選択ポップアップと一括置換ウィンドウ。表示に変化はなく、エディタの無駄な負荷が減ります）。

## [0.2.2] - 2026-08-18

### Added

- マテリアルの一括置換の各行に、マテリアルのプレビュー（サムネイル）を表示するようにしました。見本を選ぶ画面ではその行のマテリアルを、適用前の確認画面ではチェックに連動して、チェック中は置換後・未チェックは置換前のマテリアルを表示します。

### Changed

- 一括置換の確認画面の各行を 2 行表示にしました。1 行目に置換元名、2 行目に置換結果（または適用できない理由）が入り、長い名前でも読み取りやすくなります。

## [0.2.1] - 2026-08-18

### Changed

- プリセット一覧の選択（編集対象）とアクティブ状態を常に完全同期にしました。行の選択でそのプリセットがアクティブになり、ラジオでのアクティブ化には編集対象が追従します（0.1.0-beta.4 で導入した「非アクティブのまま編集」の分離仕様は撤回しました）。
- 新規プリセットの作成時、作成されたプリセットが自動で選択され、アクティブになるようにしました。追加とアクティブ状態の変更は 1 回の Undo でまとめて戻ります。

### Fixed

- マテリアルの一括置換ウィンドウが多重に開けてしまい、どのプリセット・メッシュ向けか分からなくなる問題を修正しました。開き直すと既存のウィンドウが閉じ、常に最後に押したボタンの内容 1 枚だけになります。

## [0.2.0] - 2026-08-18

### Added

- 各コンポーネントのインスペクタ上部に Materilune のロゴを表示するようにしました。Materilune マーカーを含む全コンポーネントが対象で、インスペクタの幅いっぱいにアスペクト比を保って表示されます。
- ロゴのライセンス表記 COPYING.md を追加しました。ロゴ画像は MIT ライセンスの適用外です（字体は SIL Open Font License 1.1 の League Script に基づきます）。

## [0.1.0] - 2026-08-17

最初の正式リリースです。

### Added

- プリセットのエクスポート / インポートを追加しました。1 プリセットを 1 つの `.mlsp` ファイル（JSON）として書き出し、別のプロジェクト・別のセットアップへ取り込めます。マテリアルはアセット GUID（同一ファイル内のサブアセットも区別）で照合し、見つからないものは名前とパスを添えて報告します。取り込みは新しいプリセットとして追加され、1 回の Undo で戻せます。
- プリセット一覧をドラッグで並び替えられるようにしました。
- 言語ドロップダウンに、設定が Unity Editor Localization Core を使用するツール間で共有される旨のツールチップを追加しました。

### Changed

- アクティブなプリセットを削除すると、次に表示されるプリセットが自動でアクティブになるようにしました（削除と同じ 1 回の Undo で戻ります）。
- 対象メッシュのツリーから、メッシュが自身にも子孫にも無いオブジェクト（Armature など）を非表示にしました。ツリーの最上位は常に展開された状態で開きます。
- プリセット追加ボタンを一覧タイトルの右へ移動しました。表示中のページタブのボタンは選択済み（操作不能）の表示になります。
- 新規プリセットの既定名は、使われていない最小の番号（SwapN）を採るようにしました。
- 確認ダイアログのボタンは「左が中止、右が実行」で統一しました。

### Fixed

- プリセット一覧の選択やアクティブ表示が、削除・空クリック・同名プリセットで外れたりずれたりする一連の問題を修正しました。
- 言語欄の表示が「Language」のまま残ることがある問題を修正しました。

### Removed

- 対象オブジェクトのロックトグルは仕様から削除しました（対象は Hierarchy の選択に常に追従します）。

## [0.1.0-beta.4] - 2026-08-17

### Added

- プリセット一覧の各行にアクティブ切り替えのラジオボタンを追加しました。押したプリセットだけがアクティブになり、1 回の Undo で元の状態へ戻せます。非アクティブのプリセットもビルドには含まれます。
- プリセット名をダブルクリックでその場で変更できるようにしました。Enter で確定、Esc または欄の外のクリックで取り消します。

### Changed

- プリセット一覧の行の選択は、編集対象の切り替えのみを行うようになりました。従来は選択したプリセットがアクティブ化されていましたが、アクティブ化はラジオボタンが担います。非アクティブのプリセットをアクティブ化せずに編集できます。
- 一括置換の結果一覧で、適用できる行（既存の設定を上書きする行を含む）を既定で全選択にしました。全選択・全解除ボタンも追加しています。

## [0.1.0-beta.3] - 2026-08-17

### Added

- 一括置換を追加しました。1 件の置換設定を見本として名前のパターンを検出し、パネル内の他のマテリアルへまとめて適用します。適用前に全行の結果を一覧で確認でき、既に置換先がある行は既定で対象外です。適用は 1 回の Undo で戻せます。
- 置換先の候補選択ポップアップに外枠を付けました。

### Changed

- UI のスタイル定義を UXML 自身が参照する形へ整理しました（見た目の変更はありません）。

## [0.1.0-beta.2] - 2026-08-16

### Added

- 入れ子のセットアップで、上位のセットアップが既に置き換えているマテリアルを、下位の編集画面へ読み取り専用で表示するようにしました。自分の置換先が未設定の行では置換先フィールドへ上位の値を薄く重ねて表示し、双方に設定がある行ではツールチップで示します。
- 並び順が原因で下位の設定が効かない状態を検出し、ウィンドウのステータスバーへ警告と「順序を修正」ボタンを表示するようにしました。

### Fixed

- 入れ子のセットアップで、下位の設定が上位に上書きされて適用されない問題を修正しました。Modular Avatar は収集・登録順で後に来たものを優先するため、Materilune オブジェクトを対象の先頭へ移動するようにしています。プレハブの制約で移動できない場合は移動できる範囲に留め、警告を表示します（プレハブの unpack など破壊的な操作は行いません）。

## [0.1.0-beta.1] - 2026-08-16

最初の公開版です。

### Added

- セットアップ機能。対象オブジェクトの配下へMateriluneの管理階層を生成し、Modular AvatarのMaterial Swapを配置します。
- プリセット機構。差し替えの組み合わせを複数持ち、単一のプリセットのみを有効にできます。
- 置換エントリの自動生成。対象メッシュが使用しているマテリアルからエントリを生成します。置換元は編集できず、置換先を指定したエントリのみがModular Avatarへ反映されます。
- Materiluneウィンドウ。プリセット一覧、プリセット全体の差し替え、対象メッシュのツリー、選択中メッシュの差し替えを1画面で編集できます。
- マテリアル候補の選択ポップアップ。候補をモード別のタブに分けて表示し、プレビューを添えて選べます。
- Hierarchyの行に表示するウィンドウ起動ボタン。同じ行に並ぶ他ツールのボタンとは、セッション内で共有するレジストリを介して位置を調停します。
- 各コンポーネントのインスペクタに設定の要約とウィンドウを開くボタンを表示します。Materiluneが管理するRendererには警告を表示します。
- 日本語と英語の表示切り替え。
- `Preferences > AmariNoa > Hierarchy Buttons` に、Hierarchyボタンの追加余白の設定を追加しました。

### Known limitations

- 言語設定がUnityエディタ全体・全パッケージ共通である旨の注記表示は未実装です。
- Materiluneウィンドウの対象オブジェクトを固定するロックトグルは未実装です。
- VPMリスティングへの登録先URLは確定していません。
- Modular Avatar 1.13.x での公開APIの検証は未実施です。動作確認は1.17.1で行っています。
